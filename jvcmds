
# JstVegn's User Command List
---

| Command | Description |
|---|---|
| `!sounds`, `!sound`, `!snd`, `!soundboard` | Displays the soundboard link |
| `!ttsmale`, `!tts <Phrase to say>` | Says the phrase in a male voice |
| `!ttsfemale <Phrase to say>` | Says the phrase in a female voice |
| `!es`, `!sched`, `!schedule`, `!discord` | Shows the schedule and Discord link |
| `!summon <Item to summon>` | Attempts to summon an item and delivers it to a random place |
| `!cmd`, `!cmds`, `!command`, `!commands` | Shows a list of available commands |
| `!ttp` | Shows when TTP has been reached |
| `!8ball <Question>` | Answers a question |
| `!assage` | Tells how many asses you found |
| `!fish` | Fishing game |
| `!veg` | Shows a random vegetable |
| `!recipe` | Shows a random vegan recipe |
| `!hide` | You attempt to hide |
| `!numbcharlie`, `!nc` | Shows NumbCharlie's link |
| `!nbc`, `!nebulabeans`, `!nbcoffee` | Plays a Nebula Beans Coffee commercial |
| `!lurk` | Lets chat know you are lurking |
| `!merch`, `!store` | Shows the merch store link |
| `!emo`, `!emotion` | Shows the Space Engineers emotions |
| `!quote` | Displays a random quote |
| `!joke` | Tells a random joke |
| `!game` | Lists the game being played |
| `!uptime` | Lists the uptime |
| `!zen` | Plays a peaceful video for a moment |
| `!sndlist` | Lists all the sounds |
| `!randomsound`, `!rndsnd` | Plays a random sound |

## JstVegn's User Custom Command Listing
---
| Command | Description |
|---|---|
| `!cc <Command Number>` | Runs a custom command |
| `!cchelp` | Shows how to use custom commands |
| `!listcc` | Lists your custom commands |
| `!savecc`, `!savechatcmd` | Saves a custom command for yourself |

### User Custom Command information
---
Chat Commands are your customized chat commands. It can be anything. Something you want to type or a command to run.

You can seperate messages and commands by a comma to run multiple commands, but that also means that if you have a comma in your chat message, it will be ignored and treated as a seperator.

The commands are as follows:

!cchelp (This shows this text so you know how to use Custom Commands.

!savecc 1 !quote (This will save the quote command to your first saved slot. Valid numbers are 1-20. It can be run with "!cc 1"

!listcc (This will list your set saved commands. You can have up to 20, but it will only list those that are set.

!cc <number> (This will run the specified custom command(s) that you have saved. If it is for one that is not saved, it will be ignored.
